<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "trackping" %></title>
    <%= csrf_meta_tags %>

    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>

      <div class="container">

          <header class="header">
              <%= link_to "trackping", root_path, class: "brand" %>

              <ul class="nav">
                <% unless user_signed_in? %>
                <li><%= link_to "Sign in with Twitter", user_omniauth_authorize_path(:twitter), class: 'btn btn-outline'  %></li>
                <% else %>
                <li><%= link_to 'Logout', destroy_user_session_path, :method => :delete %> </li>
                <% end %>
              </ul>
          </header>

          <div class="main">
              <%= bootstrap_flash %>
              <%= yield %>
          </div>

          <footer class="footer">
              <p>&copy; heck.io</p>
          </footer>

    </div> <!-- /container -->

  <%= javascript_include_tag "application" %>
  </body>
</html>
